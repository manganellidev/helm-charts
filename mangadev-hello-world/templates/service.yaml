apiVersion: v1
kind: Service
metadata:
  name: {{ include "mangadev-hello-world.fullname" . }}
  labels:
    {{- include "mangadev-hello-world.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "mangadev-hello-world.selectorLabels" . | nindent 4 }}
